<?php
// We use this template file so we can reuse it rather than replicating the code on every page
dynamic_sidebar( 'news-widget-area' );
	// dynamic_sidebar( 'helpdesk-buttons-widget-area' );
	// dynamic_sidebar( 'weekly-notices-widget-area' );
    // dynamic_sidebar( 'remote-learning-widget-area' );
    // dynamic_sidebar( 'useful-docs-widget-area' );
	// dynamic_sidebar( 'medical-widget-area' );
	// dynamic_sidebar( 'data-collection-widget-area' );
	// dynamic_sidebar( 'magt-widget-area' );
	// dynamic_sidebar( 'social-events-widget-area' );
	
	// dynamic_sidebar( 'lost-n-found-widget-area' );
	dynamic_sidebar( 'search-widget-area' );
?>